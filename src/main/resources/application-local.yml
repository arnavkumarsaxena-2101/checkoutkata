spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/checkout
    username: checkout
    password: checkout
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

server:
  port: 8080