<configuration scan="true">

    <!-- ===== Appenders ===== -->

    <!-- Human-friendly text for local dev -->
    <appender name="CONSOLE_TEXT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Structured JSON for containers/ELK -->
    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <version/>
                <level/>
                <threadName/>
                <loggerName/>
                <message/>
                <arguments/>
                <mdc/>        <!-- requestId, traceId/spanId, etc. -->
                <stackTrace/>
            </providers>
        </encoder>
    </appender>

    <!-- Async wrappers -->
    <appender name="ASYNC_TEXT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="CONSOLE_TEXT"/>
        <queueSize>8192</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <appender name="ASYNC_JSON" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="CONSOLE_JSON"/>
        <queueSize>8192</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- ===== Root logger selection via Spring profile ===== -->

    <!-- Default: text -->
    <springProfile name="!json-logs">
        <root level="INFO">
            <appender-ref ref="ASYNC_TEXT"/>
        </root>
    </springProfile>

    <!-- When profile 'json-logs' is active -->
    <springProfile name="json-logs">
        <root level="INFO">
            <appender-ref ref="ASYNC_JSON"/>
        </root>
    </springProfile>

    <!-- Tame noisy libs -->
    <logger name="org.hibernate.SQL" level="INFO"/>
    <logger name="org.hibernate.type.descriptor.sql" level="WARN"/>
    <logger name="org.testcontainers" level="WARN"/>

</configuration>
